# Cách chạy ứng dụng
- Chạy lệnh ```docker-compose up --build``` để build và chạy
- Vào máy client bằng lệnh ```docker exec -it note_client bash```, sau đó chạy lệnh ```./client_app``` để khởi chạy client
- Khởi động lại máy ```docker-compose restart```
